[pytest]
asyncio_mode = auto
# Pin the fixture loop scope explicitly: pytest-asyncio 1.x warns when this is
# unset and will change its default to "function" in a future release.
asyncio_default_fixture_loop_scope = function
testpaths = tests
